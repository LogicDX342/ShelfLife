{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "dropzone",
  "description": "Minimal capability for the dropzone overlay window",
  "windows": ["dropzone"],
  "permissions": ["core:default", "core:window:default"]
}
